<% include ../partials/header %>

  <div class="row">
    <!--<div class="col-md-3">-->
    <!--  <p class="lead">RenchTech</p>-->
      <!--<div class="list-group">-->
      <!--  <li class="list-group-item active">My Projects</li>-->
      <!--  <li class="list-group-item">Community Projects</li>-->
      <!--  <li class="list-group-item">3</li>-->
      <!--</div>-->
      <!--MAP-->
    <!--</div>-->
    <div class="col-md-12">
      <div class="thumbnail">
        <h4><a><%=project.name%></a></h4>
        <img class="img-responsive" src="<%= project.image %>">
        <div class="caption-full">
          <!--<h4><a><%=project.name%></a></h4>-->
          <br>
          <p><%= project.description %></p>
          <br>
          <!--<p><iframe width="560" height="315" src="https://www.youtube.com/embed/Asgdg3nJt58" frameborder="0" allow="autoplay; encrypted-media" allowfullscreen></iframe></p>-->
          <p><iframe width="560" height="315" src="<%=project.video%>" frameborder="0" allow="autoplay; encrypted-media" allowfullscreen></iframe></p>
          <p>
            <em>Submitted by: <%= project.author.username %>, <%= moment(project.createdAt).fromNow() %></em>
          </p>
          <% if(currentUser && project.author.id.equals(currentUser._id) || currentUser && currentUser.isAdmin){ %>
            <a class="btn btn-xs btn-warning" href="/projects/<%= project._id %>/edit">Edit</a>
            <form class="delete-form" action="/projects/<%= project._id %>?_method=DELETE" method="POST">
              <button class="btn btn-xs btn-danger">DELETE</button>
          </form>
          <% } %>
        </div>
      </div>
      <div class="well">
        <div class="text-right">
          <a class="btn btn-success" href="/projects/<%= project._id %>/comments/new">Add New Comment</a>
        </div>
        <hr>
        <% project.comments.forEach(function(comment){ %>
          <div class="row">
            <div class="col-md-12">
              <strong><%= comment.author.username %> </strong>
              <span class="pull-right"><%= moment(comment.createdAt).fromNow() %></span>
              <p>
                <%= comment.text %>
              </p>
            <% if(currentUser && comment.author.id.equals(currentUser._id)){ %>
              <a class="btn btn-xs btn-warning" href="/projects/<%= project._id %>/comments/<%=comment._id %>/edit">
                Edit
              </a>
              <form class="delete-form" action="/projects/<%= project._id %>/comments/<%=comment._id %>?_method=DELETE" method="POST" >
                <input type="submit" class="btn btn-xs btn-danger" value="Delete">
              </form>
            <% } %>
            </div>
          </div>
          
        <% }) %>
      </div>
    </div>
  </div>


<% include ../partials/footer %>